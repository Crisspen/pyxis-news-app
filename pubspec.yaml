name: pyxis_news
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  hive: ^2.2.2
  hive_flutter: ^1.1.0
  http: ^1.2.2
  intl: ^0.19.0
  provider: ^6.1.2
  share_plus: ^10.0.2
  shimmer: ^3.0.0
  webview_flutter: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.12
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/empty.png
